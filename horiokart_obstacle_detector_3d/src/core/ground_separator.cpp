#include "horiokart_obstacle_detector_3d/core/ground_separator.hpp"

// avoid using-directive in implementation file; symbols referenced via fully
// qualified names or from the header.

// all implementation in header (minimal). This cpp exists to allow linking in
// some build systems.
